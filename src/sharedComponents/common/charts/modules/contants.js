export const LOW_MED_HIGH = 'lowmedhigh';

export const PLOT_BANDS_TYPE = Object.freeze({
    off_hours: 'off_hours',
    after_hours: 'after_hours',
});

export const LOW_MED_HIGH_TYPES = Object.freeze({
    LOW: 'low',
    MED: 'med',
    HIGH: 'high',
});
